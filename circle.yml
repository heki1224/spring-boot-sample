general:
  artifacts:
    - "target/spring-boot-sample-*.jar"
machine:
  java:
    version: oraclejdk8
dependencies:
  override:
    - mvn dependency:resolve
test:
  override:
    - mvn integration-test
deployment:
  release:
    branch: master
    commands:
      - mvn package